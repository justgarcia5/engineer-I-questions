<div class="edit-contact-div">
  <h1 class="edit-title">Edit Contact</h1>
  <% if current_user.id == @contact.user_id %>
    <%= render "form" %>
  <% else %>
    <h1 style="text-align: center">Not found!</h1>
  <% end %>
</div>
